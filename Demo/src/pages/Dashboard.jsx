import { useEffect,useState } from 'react';
import { FileText, Image, HardDrive } from 'lucide-react';
import { collection,onSnapshot,orderBy,query } from 'firebase/firestore';
import { db } from '../services/firebase';
export default function Dashboard(){const [notes,setNotes]=useState([]),[pictures,setPictures]=useState([]);useEffect(()=>{const a=onSnapshot(query(collection(db,'notes'),orderBy('updatedAt','desc')),s=>setNotes(s.docs.map(d=>({id:d.id,...d.data()}))));const b=onSnapshot(query(collection(db,'pictures'),orderBy('createdAt','desc')),s=>setPictures(s.docs.map(d=>({id:d.id,...d.data()}))));return()=>{a();b()}},[]);return <div><div className="dashboard-header"><h1>Welcome to NovaCloud</h1><p>Your personal cloud workspace.</p></div><div className="stats"><div className="stat-card"><div className="stat-icon"><FileText/></div><div><p>Total Notes</p><h2>{notes.length}</h2></div></div><div className="stat-card"><div className="stat-icon"><Image/></div><div><p>Pictures</p><h2>{pictures.length}</h2></div></div><div className="stat-card"><div className="stat-icon"><HardDrive/></div><div><p>Cloud Sync</p><h2>Active</h2></div></div></div><section className="dashboard-section"><h2>Recent Notes</h2>{notes.length===0?<div className="empty-state"><FileText size={45}/><h3>No notes yet</h3><p>Go to Notes and create your first cloud note.</p></div>:<div className="notes-grid">{notes.slice(0,6).map(n=><div className="note-card" key={n.id}><h3>{n.title}</h3><p>{n.content||'No content'}</p><small>{new Date(n.updatedAt).toLocaleDateString()}</small></div>)}</div>}</section></div>}
